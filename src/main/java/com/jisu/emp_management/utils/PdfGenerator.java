package com.jisu.emp_management.utils;import com.jisu.emp_management.model.Employee;import com.lowagie.text.*;import com.lowagie.text.Font;import com.lowagie.text.Image;import com.lowagie.text.Rectangle;import com.lowagie.text.pdf.PdfPCell;import com.lowagie.text.pdf.PdfPTable;import com.lowagie.text.pdf.PdfWriter;import java.awt.*;import java.io.ByteArrayOutputStream;import java.io.File;import java.io.FileOutputStream;import java.io.IOException;import java.util.ArrayList;public class PdfGenerator {    public static File generatePDF(Employee emp) throws DocumentException, IOException {        ByteArrayOutputStream outputStream = new ByteArrayOutputStream();        Document document = new Document();        PdfWriter.getInstance(document, outputStream);        HeaderFooter footer=new HeaderFooter(true,new Phrase("© 2023 jisu2000. All Rights Reserved."));        footer.setAlignment(Element.ALIGN_CENTER);        footer.setBorderWidthBottom(0);        document.setFooter(footer);        document.open();        // Title        Font titleFont = FontFactory.getFont(FontFactory.HELVETICA_BOLD, 20, Color.BLUE);        Paragraph title = new Paragraph("Employee Details", titleFont);        title.setAlignment(Element.ALIGN_CENTER);        document.add(title);        // Employee image and details table        PdfPTable employeeDetailsTable = new PdfPTable(2);        employeeDetailsTable.setWidthPercentage(100);        employeeDetailsTable.setSpacingBefore(10f);        employeeDetailsTable.setSpacingAfter(10f);        // Employee Image Cell        PdfPCell imageCell = new PdfPCell();        imageCell.setBorder(Rectangle.NO_BORDER);        imageCell.setHorizontalAlignment(Element.ALIGN_LEFT);        Image pdfImage = Image.getInstance(emp.getImage().getData());        pdfImage.scaleToFit(120, 160);        imageCell.addElement(pdfImage);        // Employee Details Cell        PdfPCell detailsCell = new PdfPCell();        detailsCell.setBorder(Rectangle.NO_BORDER);        detailsCell.addElement(new Paragraph("Name: " + emp.getName()));        detailsCell.addElement(new Paragraph("Email: " + emp.getEmail()));        detailsCell.addElement(new Paragraph("Registration Date: " + emp.getDate()));        detailsCell.addElement(new Paragraph("Department: " + emp.getDept().getDeptName()));        detailsCell.addElement(new Paragraph("Contact No: " + emp.getContact()));        detailsCell.addElement(new Paragraph("Address: " + emp.getAddress() + ", " + emp.getPincode()));        employeeDetailsTable.addCell(imageCell);        employeeDetailsTable.addCell(detailsCell);        document.add(employeeDetailsTable);        String  pr= "                                                                                            ";        String sp="";        for(int i=0;i<30;i++){            sp+=pr;        }        Paragraph space=new Paragraph(sp);        document.add(space);        // Terms & Conditions        Font ruleFont = FontFactory.getFont(FontFactory.HELVETICA, 15, Color.RED);        Paragraph ruleTitle = new Paragraph("Terms & Conditions", ruleFont);        ruleTitle.setAlignment(Element.ALIGN_LEFT);        document.add(ruleTitle);        String[] rulesArray = {                "Any violation of company IT policies may result in disciplinary action.",                "All data stored on company systems is subject to monitoring.",                "Employees must adhere to password security guidelines.",                "Unauthorized access to confidential data is strictly prohibited.",                "Use of company resources for personal purposes is limited.",                "Any suspicious activity should be reported immediately.",                "Compliance with all applicable laws and regulations is mandatory.",                "Please be further informed that existence of an offer or offering any employment.",                "Internal information should be kept private."        };        // Adding individual rules as paragraphs        int i=1;        for (String ruleStr : rulesArray) {            Paragraph ruleParagraph = new Paragraph(i++ +". "+ruleStr);            ruleParagraph.setIndentationLeft(20); // Indent the rules for better readability            document.add(ruleParagraph);        }        document.close();        // Write PDF content to file        File pdfFile = new File("EmployeeDetails.pdf");        FileOutputStream fos = new FileOutputStream(pdfFile);        outputStream.writeTo(fos);        fos.close();        return pdfFile;    }}